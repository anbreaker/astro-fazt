---
import Card from '../components/card.astro';

const response = await fetch('https://jsonplaceholder.typicode.com/posts');

const posts = await response.json();
---

<h1>Mis Posts</h1>
{posts.map((post) => <Card body={post.body} id={post.id} title={post.title} />)}
